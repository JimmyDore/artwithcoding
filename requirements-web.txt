# Web-compatible requirements for pygbag deployment
# Only pygame is required for the core functionality

pygame==2.6.1

# Optional dependencies for non-web mode
# (these will be gracefully disabled in web mode)
# numpy>=1.21.0
# imageio>=2.34.0
# pyaudio>=0.2.11
# scipy>=1.9.0